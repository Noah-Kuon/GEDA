VS CodeでGitHubの接続アカウントを切り替える手順をまとめたMarkdownファイルを作成しました。

Your Markdown file is ready
[file-tag: code-generated-file-0-1782717166416053206]

作成したファイルの内容は以下の通りです。

---

# VS CodeでGitHubの接続アカウントを切り替える手順

VS Code（Visual Studio Code）で、現在連携しているGitHubアカウントから別のアカウント（副アカウントや会社用など）に切り替える手順を解説します。

VS Code自体のアカウント連携と、パソコンのシステム側（認証情報マネージャーやキーチェーン）に残っている情報の両方をクリアするのが確実な方法です。

---

## 流れの全体像
1. VS Code上のアカウントからサインアウト
2. パソコン内の古い認証情報を削除（リセット）
3. 新しいアカウントでサインイン（再連携）

---

## 🛠 ステップ 1: VS Code上のアカウントからサインアウトする

まずは、VS Codeが記憶しているGitHubアカウントを解除します。

1. VS Codeの画面左下にある **「⚙️ 歯車アイコン（または人の形をしたアカウントアイコン）」** をクリックします。
2. メニューの中から **「[アカウント名] (GitHub)」** という項目を探し、それをクリックします。
3. 表示されたメニューから **「サインアウト (Sign Out)」** をクリックします。

---

## 🛠 ステップ 2: パソコン内の古い認証情報を削除する

VS Codeからサインアウトしても、パソコン本体（OS）が古いログイン情報を記憶しているため、自動的に前のアカウントで繋がってしまうことがあります。これを防ぐために古い情報を削除します。

### 💻 Windowsの場合
1. スタートメニューの検索窓に「**資格情報マネージャー**」と入力して開きます。
2. 「**Windows 資格情報**」をクリックします。
3. 汎用資格情報の欄にある **`vscode://vscode.github-authentication/...`** や **`git:https://github.com`** という名前の項目を探します。
4. 項目を展開し、**「削除」** をクリックします。

### 🍎 Macの場合
1. アプリケーション一覧（またはSpotlight検索）から「**キーチェーンアクセス (Keychain Access)**」を開きます。
2. 右上の検索窓に「**github**」と入力します。
3. 種類が「インターネットパスワード」や「アプリケーションパスワード」になっている **`vscode.github-authentication`** や **`github.com`** の項目を見つけます。
4. 右クリック（または副ボタンクリック）して **「削除」** を選択します。

---

## 🛠 ステップ 3: 新しいアカウントでサインインする

準備が整ったら、新しいアカウントで接続します。

1. 変更したいリポジトリのデータをVS Codeで開く、または新しくクローン（コピー）を試みます。
2. 同期やプッシュ、クローンなどを行う際に、VS Codeの上部に「**GitHubが拡張機能にサインインすることを求めています**」といったポップアップが表示されます。
3. **「許可 (Allow)」** をクリックすると、ブラウザが起動します。
4. ブラウザ側で**新しく切り替えたい方のGitHubアカウント**でログインし、連携を承認（Authorize）します。
5. VS Codeに戻ると、自動的に新しいアカウントでの接続が完了します。

---

### 💡 うまく切り替わらない場合のチェックポイント
* **ブラウザのログイン状態:** ブラウザ（Chromeなど）で事前に古いGitHubアカウントからサインアウトし、新しいアカウントでログインした状態にしてからステップ3の連携を行うとスムーズです。
* **Gitのユーザー名設定:** コマンドでコミット（保存）する際の「名前」と「メールアドレス」も新しいアカウントに合わせたい場合は、VS Codeのターミナルで以下のコマンドを実行してください。
  ```bash
  git config --global user.name "新しいユーザー名"
  git config --global user.email "新しいメールアドレス"
