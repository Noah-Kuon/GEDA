# 🚀 ホームページ更新・反映手順書

今後、コードの修正や新しいファイルの追加を行った際は、以下の手順を上から順番に実行するだけで自動的にVercel（ホームページ）へ反映されます。


1. 今の「待ち状態」を強制終了する
キーボードの Ctrl キーを押しながら C を押してください。（Ctrl + C）

💡 これを押すと、画面がリセットされて、いつもの KonoMacBook-puro:GEDA komiyazaki$ という表示に戻ります。
---

## 🛠️ ステップ 1：VS Codeでファイルを修正・保存する
1. HTMLやCSS、JavaScriptなどのファイルを修正します。
2. 必ず変更したファイルを **保存（`Ctrl + S`）** してください。

> 💡 **注意点**
> 最新のHTML（例: `qcard_pro_v2_5_4.html` など）を新しく追加した場合は、必ず **`app.html`** の一番下にある `iframe.src = '/最新ファイル名.html'` の部分も一緒に書き換えて保存してください。

---

① まず「GEDA」フォルダの中に移動します（重要！）

Bash
cd GEDA


## 🚀 ステップ 2：ターミナルで3つのコマンドを実行する

VS Codeのターミナル（PowerShell）を開き、以下の **3つのコマンドを1行ずつコピペして実行** します。

### ① 変更したファイルをすべて準備する
```bash
git add .


② 今回の変更内容にメモ（コミットメッセージ）をつける
" " の中身は、何を変えたか分かるように自由に変更して大丈夫です。

Bash
git commit -m "サイトの表示を更新"


③ GitHub（Vercel）へ送信する

Bash
git push origin main


👀 ステップ 3：確認する
git push が成功したら、1〜2分ほど待ってから いつもの公開URLにアクセスして、変更が反映されているか確認してください。

🌐 公開URL: https://geda-project.vercel.app
https://qcard-pro.vercel.app/

---

### ✨ ワンポイント・アドバイス
拡張子が `.md`（マークダウン）のファイルは、VS Codeで開いたときに画面右上にある **「プレビューボタン（本のマークに虫眼鏡がついたようなアイコン）」** を押すと、ホームページのように綺麗に装飾された状態で読むことができます。
