VS Codeを使ったリポジトリのコピー手順
VS Codeの便利な機能を使って、GitHubのリポジトリをパソコンにコピー（クローン）する手順です。

🛠 方法1：VS Codeの「機能」を使ってコピーする（一番簡単）
コマンドを入力せず、VS Codeの画面操作だけでコピーする方法です。

ステップ 1: GitHubでURLをコピーする
ブラウザでリポジトリのページを開き、緑色の「<> Code」ボタンをクリックします。

Local タブの「HTTPS」のURLをコピーします。

ステップ 2: VS Codeでクローンを開始する
VS Codeを開きます。

左側の一番上にある「エクスプローラー（ファイルのアイコン）」をクリックします。

青いボタン「リポジトリのクローン (Clone Repository)」をクリックします。

※もしボタンが表示されていない場合は、キーボードの Ctrl + Shift + P（Macは Cmd + Shift + P）を押して、上の検索窓に Git: Clone と入力して選択してください。

ステップ 3: URLの貼り付けと保存場所の選択
VS Codeの上部に表示される入力欄に、先ほどコピーした GitHubのURLを貼り付け して Enter キーを押します。

パソコンのフォルダ選択画面が開くので、保存したい場所（「デスクトップ」や「ドキュメント」など） を選んで、「リポジトリの場所として選択」をクリックします。

ステップ 4: 完了
右下に「クローンしたリポジトリを開きますか？」とポップアップが出るので、「開く (Open)」をクリックすれば、そのままVS Codeで編集を始められます！

💻 方法2：VS Code内の「ターミナル」を使ってコピーする
先ほどのコマンド手順を、VS Codeの中で実行する方法です。

ステップ 1: VS Codeでターミナルを開く
VS Codeの上部メニューから「ターミナル (Terminal)」＞「新しいターミナル (New Terminal)」をクリックします。

画面の下側に黒い画面（ターミナル）が表示されます。

ステップ 2: コマンドを実行する
データの保存場所に移動します（例：デスクトップにする場合）。

Bash
cd ~/Desktop
クローンコマンドを実行します。

Bash
git clone [GitHubでコピーしたURL]
完了したら、VS Codeの「ファイル」＞「フォルダーを開く」から、デスクトップにできたフォルダを選択して開きます。
